create_table("transactions") {
    t.Column("transaction_id", "integer", {primary: true})
    t.Column("customer_id", "string", {})
    t.Column("transaction_number", "string", {})
    t.Column("code_id", "string", {})
    t.Column("total_amount", "bigint", {})
    t.Column("created_at", "timestamp", {"null": true})
    t.Column("updated_at", "timestamp", {"null": true})
}