create_table("carts") {
    t.Column("cart_id", "integer", {primary: true})
    t.Column("id", "integer", {})
    t.Column("product_id", "integer", {})
    t.Column("code_id", "integer", {})
    t.Column("quantity", "integer", {})
    t.Column("cart_total_amount", "bigint", {})
    t.Column("created_at", "timestamp", {"null": true})
    t.Column("updated_at", "timestamp", {"null": true})
}